\ProvidesPackage{preamble}


\usepackage{polyglossia}
\setdefaultlanguage{vietnamese}

\usepackage[final]{microtype}
\usepackage{lmodern}

% Clickable link
\usepackage[vietnamese]{hyperref}    % This sucker ignore polyglossia

\usepackage{amsthm}

% Graphics
\usepackage{graphicx}
\usepackage{svg}
\usepackage{wrapfig}

% Code
\usepackage{minted}      % This one is used for syntax highlighting
\usepackage{listings}    % This one is used to replace verbatim, not for syntax highlighting
\lstset{
  basicstyle=\ttfamily,
  columns=fullflexible,
%  keepspaces=true,
  escapeinside={\%*}{*)},    % Cheeky way to escape UTF-8 to LaTeX, https://tex.stackexchange.com/a/24544/206774
}

\usepackage{float}

% Directory tree symbols
\usepackage{pmboxdraw}

% Table shit
\usepackage{booktabs}

% Caption
\usepackage{caption}
\captionsetup[table]{skip=5pt}    % Tweak to avoid table being too close to caption

% References
\providecommand{\main}{.}    % https://tex.stackexchange.com/a/216321/206774
\usepackage{biblatex}
\addbibresource{\main/references.bib}

\usepackage[figure, table]{totalcount}

\usepackage{enumitem}


% https://uet.vnu.edu.vn/wp-content/uploads/2017/07/Quy-%C4%91i%CC%A3nh-v%C3%AA%CC%80-tri%CC%80nh-ba%CC%80y-kho%CC%81a-lu%C3%A2%CC%A3n-t%C3%B4%CC%81t-nghi%C3%AA%CC%A3pCLC.pdf
\usepackage[top=2.5cm, bottom=3.0cm, left=3.0cm, right=2.0cm]{geometry}
\renewcommand{\baselinestretch}{1.3}


% Blank page
% https://tex.stackexchange.com/a/374542/206774
\def\blankpage{%
  \clearpage%
  \thispagestyle{empty}%
  \addtocounter{page}{-1}%
  \null%
  \clearpage}

% Temporarily change margins
% https://tex.stackexchange.com/a/600/206774
\def\changemargin#1#2{\list{}{\rightmargin#2\leftmargin#1}\item[]}
\let\endchangemargin=\endlist


\usepackage{subfiles} % Best loaded last in the preamble


\author{Nguyễn Việt Minh Nghĩa}
\title{ỨNG DỤNG ĐỌC TRUYỆN TRANH}
